angular.module('index', [])
  .controller('IndexCtrl', ['$scope', '$http', function($scope, $http) {

    $http.jsonp('http://filltext.com/?rows=10&fname={firstName}&lname={lastName}&tel={phone}&callback=JSON_CALLBACK').success(function(data){
      $scope.people = data;
    })
    
  }])