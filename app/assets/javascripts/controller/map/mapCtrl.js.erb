angular.module('map', ['google-maps'])
  .controller('MapCtrl', ['$scope', function ($scope) {
    $scope.map = {center: {latitude: 37.4, longitude: -122 }, zoom: 10 };
    $scope.options = {scrollwheel: false};
  }])

  